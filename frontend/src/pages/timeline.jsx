import React from "react";
import styled from "styled-components";

const Timeline = () => {
  return (
    <Styles>
      <h1>{`UNDER CONSTRUCTION`}</h1>
      <h4>{`Timeline Gantt View`}</h4>
      <p>This page will display a Gantt timelin view for that shows the duration and dependency between action items</p>
    </Styles>
  );
};

const Styles = styled.div`
  padding: 20px;
`;

export default Timeline;
