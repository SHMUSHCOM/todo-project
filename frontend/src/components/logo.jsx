import React from 'react';
import styled from 'styled-components'

const Logo = () => {
    return <Styles>
        <img src="/Logo.png" alt="Shmush Logo" />
    </Styles>
}

const Styles = styled.div`
    
`

export default Logo;
